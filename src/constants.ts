export const routes = {
  home: { path: "/" as const, id: "home" },
};

export const WORDS_COUNT = 20 as const;
export const PUNCTUATION_CHANCE = 0.25 as const;
export const CAPITALIZE_CHANCE = 0.25 as const;
export const TIMER_SECONDS_COUNT = 30;
